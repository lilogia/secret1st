<%secrets.forEach(item => {%>
    <p>*** <%= item.secret%> | <a href="edit?secret=<%= item.secret%>">Edit</a>, <a href="delete?secret=<%= item.secret%>">Delete</a></p>
    <p><%= item.url%></p>
    <p><%= item.name%></p>
    <p>??<%= item.pwd%>??</p>
<%});%>